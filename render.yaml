services:
  - type: web
    name: civegpt-backend
    env: python
    pythonVersion: '3.9'
    region: oregon
    plan: free
    build: pip install -r backend/requirements.txt
    start: python backend/main.py
    envVars:
      - key: GROQ_API_KEY
        sync: false
